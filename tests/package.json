{
  "name": "github-actions-self-test",
  "private": true,
  "description": "Self-test fixture exercised by .github/workflows/ci.yml. Not published.",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "format": "echo 'format ok'",
    "lint": "echo 'lint ok'",
    "build": "echo 'build ok'",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "jest": "30.0.0"
  }
}
